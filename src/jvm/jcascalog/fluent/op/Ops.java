package jcascalog.fluent.op;



// TODO we might consider removing jcascalog.op
// we don't need subclasses only instances with different parameters
// XXX 'inheritance' of multiple (static) help classes sucks but what can we do?
// the other solution is namespace extension with composition
// ie private BooleanOps bo = new BooleanOps();
// and later
// bo.lt();
public class Ops extends SpecialOps {

}
